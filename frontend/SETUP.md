
# フロントエンド構築メモ

## Next.jsアプリ作成

```zsh
yarn create next-app --typescript

# プロジェクト名は`kakeibo`にすr
success Installed "create-next-app@12.1.0" with binaries:
      - create-next-app
✔ What is your project named? … kakeibo
```

## ルートディレクトリの名前を変更

```zsh
mv kakeibo frontend

# 移動
cd frontend
```

## 
